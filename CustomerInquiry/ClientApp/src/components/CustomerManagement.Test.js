import 'jest';
import React from 'react';
import CustomerManagement from './CustomerManagement';

describe('CustomerManagement', () => {
    it('should render correctly customers table', () => {
        expect(2 + 2).toBe(4);
    });
});